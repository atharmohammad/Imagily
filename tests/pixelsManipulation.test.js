const ImageToUri = require('../src/modules/pixelsManipulation')

describe('Pixels breakdown', () => {
  it('Gets the pixel array from an image', () => {
    // const img = new Image()
    // let imageWidth = 0
    // let imageHeight = 0

    // ImageToUri('../Images/car1.jpg', function (uri) {
    //   img.src = uri
    // })
    // console.log(imageWidth, imageHeight)
  })
})
